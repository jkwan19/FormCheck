import React, { useState, useEffect } from 'react';

function PlanksProgress (props) {
  // let planksProgress = props.progress.filter((data) => data.workout.includes("Planks"));
  return(
    <div>
      Planks Progress
    </div>
  )
}

export default PlanksProgress;